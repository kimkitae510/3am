package com.threeam.llm;

// 같은 프로바이더 안에서 호출이 무엇을 하러 가는지. 모델, thinking 세기, 타임아웃이 여기서 갈린다.
public enum LlmTier {

    // 상담 답변과 사실 추출. 서비스의 본체라 강한 모델을 쓴다.
    CHAT,

    // 재회 진단. 긴 루브릭을 다단으로 밟아야 해서 추론을 가장 많이 준다.
    DEEP,

    // 저가 판별. 상담이 아니라 "이 말이 어느 갈래인가"만 가린다 —
    // 강한 모델도 긴 추론도 값을 안 한다. 추론 토큰이 곧 비용이라 여기를 낮추는 게 곧 절감이다.
    QUICK
}
